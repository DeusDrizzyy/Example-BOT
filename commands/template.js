module.exports.run = (bot, message, args) => {
   // Your code....
};

module.exports.configs = {
    // Enter the command name without spaces. ( Required )
    name: "template",
    // Type the command aliases without spaces. ( Optional )
    aliases: ['template1', 'template2'],
    // Enter the ID of the Discord channels you want the command to work on. ( Optional )
    allowedChats: ['000000000000000', '000000000000002']
};
